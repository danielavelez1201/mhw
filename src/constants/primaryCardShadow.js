import theme from '../theme';

const primaryCardShadow = {
  shadowColor: theme.colors.primary[300],
  shadowOffset: {
    width: 0,
    height: 4,
  },
  shadowOpacity: 0.5,
  shadowRadius: 4.65,
  elevation: 8,
};

export default primaryCardShadow;
